# 0 基础 PLC 工程师如何用 AI 学习和开发

适用对象：第一次接触 PLC、正在厂里做第一个 PLC 项目的人  
背景身份：有一点编程意识，但没有 PLC 经验，也没有 AI 使用经验  
当前软件：类似三菱 GX Works / GX Developer 风格的 PLC 梯形图编程软件

---

## 一、先说结论

你现在用的软件不是普通代码编辑器，也不是简单的拖拽式编程工具。

它是 PLC 编程软件，里面主要写的是 **梯形图**。

PLC 程序的作用是：

```text
接收按钮、传感器等输入信号
经过 PLC 内部程序判断
再控制电机、电磁阀、气缸、报警灯等输出设备
```

可以简单理解成：

```text
输入信号 X
↓
PLC 程序逻辑
↓
内部继电器 M / 定时器 T / 数据寄存器 D
↓
输出信号 Y
↓
真实设备动作
```

AI 可以帮你学 PLC，也可以帮你生成 PLC 程序草案，但不能直接代替你把程序下载到设备上运行。

最安全、最现实的用法是：

```text
AI 帮你看懂旧程序
AI 帮你把梯形图翻译成普通逻辑
AI 帮你根据需求设计程序草案
你在 PLC 软件里实现
现场工程师或师傅确认
最后再调试设备
```

---

## 二、PLC 到底是什么

PLC 全称是：

```text
Programmable Logic Controller
可编程逻辑控制器
```

它本质上是工业现场用的控制电脑。

它不像普通电脑那样主要跑软件界面，而是专门用来控制机器。

比如：

```text
按钮按下
传感器检测到物料
气缸到位
温度超过设定值
电机运行完成
```

这些信号会进入 PLC。

PLC 根据程序判断之后，再控制：

```text
电机启动
电机停止
气缸前进
气缸后退
报警灯亮
蜂鸣器响
输送带运行
```

一句话：

```text
PLC 是工厂设备的大脑。
```

---

## 三、PLC 程序和普通代码有什么不同

如果你写过 C、Java、Python 这类程序，通常是写一行行代码。

但 PLC 常见程序是梯形图，看起来像电路图。

比如：

```text
启动按钮        停止按钮        电机
--| |------------|/|------------( )
```

可以理解成普通代码：

```c
if (启动按钮 == ON && 停止按钮 == OFF) {
    电机 = ON;
}
```

所以梯形图不是没有代码，而是代码的表现形式不一样。

它把逻辑画成了类似电气线路的样子。

---

## 四、你截图里的软件大概是什么

你发的截图里能看到：

```text
AUTO [PRG] [LD]
INPUT [PRG] [LD]
OUTPUT [PRG] [LD]
ALARM [PRG] [LD]
COMMENT
M62
T22
SM400
SET
RST
ZRST
OUT
END
```

这些都是三菱 PLC 软件里非常典型的内容。

大概率是：

```text
GX Works2
GX Developer
或类似三菱 PLC 编程环境
```

其中：

```text
LD = Ladder Diagram，梯形图
PRG = Program，程序
AUTO = 自动程序
INPUT = 输入处理
OUTPUT = 输出处理
ALARM = 报警处理
COMMENT = 注释
```

你可以把它理解成普通软件项目里的模块：

```text
INPUT  类似 input.c，处理输入信号
AUTO   类似 auto.c，处理自动运行流程
OUTPUT 类似 output.c，处理输出控制
ALARM  类似 alarm.c，处理报警
```

---

## 五、PLC 里常见的地址是什么意思

三菱 PLC 里经常会看到这些符号。

| 符号 | 含义 | 类比 |
|---|---|---|
| X | 输入点 | 按钮、传感器输入 |
| Y | 输出点 | 电机、电磁阀、报警灯输出 |
| M | 内部继电器 | bool 变量 |
| D | 数据寄存器 | int / word 变量 |
| T | 定时器 | timer |
| C | 计数器 | counter |
| SM | 特殊辅助继电器 | 系统状态位 |
| SD | 特殊数据寄存器 | 系统数据 |

比如：

```text
X0  启动按钮
X1  停止按钮
Y0  输送电机
Y1  气缸前进阀
M100 自动运行状态
T0  延时定时器
D0  产量计数或参数值
```

---

## 六、常见 PLC 指令是什么意思

### 1. OUT

```text
OUT Y0
```

意思是输出线圈。

可以理解成：

```c
Y0 = 当前条件;
```

如果前面的条件成立，Y0 就输出。

---

### 2. SET

```text
SET M100
```

意思是置位。

可以理解成：

```c
M100 = true;
```

SET 之后，即使前面的条件消失，M100 也会保持 ON，直到被 RST 复位。

---

### 3. RST

```text
RST M100
```

意思是复位。

可以理解成：

```c
M100 = false;
```

---

### 4. ZRST

```text
ZRST M100 M107
```

意思是批量复位。

可以理解成：

```c
M100 = false;
M101 = false;
M102 = false;
M103 = false;
M104 = false;
M105 = false;
M106 = false;
M107 = false;
```

---

### 5. 定时器 T

```text
OUT T22 K10
```

意思是启动 T22 定时器，设定值是 K10。

实际时间要看 PLC 型号和定时器基准。

有些情况下 K10 可能表示 1 秒，有些情况下可能不是，所以不能盲目确定。

---

## 七、PLC 的运行方式

PLC 不是程序执行一次就结束。

它会一直循环扫描。

可以理解成：

```c
while (PLC正在运行) {
    读取所有输入;
    执行梯形图程序;
    刷新所有输出;
}
```

这个过程叫：

```text
PLC 扫描周期
```

你以后看 PLC 程序，一定要记住：

```text
梯形图是不断循环执行的。
```

---

## 八、你截图里那段程序大概在做什么

截图里能看到类似逻辑：

```text
SET M62
M62 -> OUT T22 K10
T22 -> RST M62
SM400 + M900 -> ZRST M100 M107
RST M201
END
```

翻译成普通逻辑大概是：

```c
// 某个条件触发后，置位 M62
M62 = true;

// 如果 M62 为 ON，启动 T22 定时器
if (M62) {
    start_timer(T22, K10);
}

// T22 到时后，复位 M62
if (T22.done) {
    M62 = false;
}

// 如果 PLC 常 ON 条件成立，并且 M900 成立
if (SM400 && M900) {
    M100 = false;
    M101 = false;
    M102 = false;
    M103 = false;
    M104 = false;
    M105 = false;
    M106 = false;
    M107 = false;

    M201 = false;
}
```

这段程序可能是在做：

```text
短时间蜂鸣
状态复位
自动流程步清零
报警后或某条件下复位部分状态
```

但是必须注意：

```text
具体含义要看项目注释、I/O 表、设备图纸和现场动作。
```

AI 可以帮你猜逻辑，但不能替你确认现场设备。

---

## 九、流程图和 PLC 程序有什么关系

你朋友问到：

```text
那些流程图和代码有关系吗？
```

答案是：

```text
有关系。
```

但它们不是同一个东西。

### 流程图

流程图是给人看的。

比如：

```text
按启动
输送带运行
检测到物料
气缸前进
前进到位
延时 2 秒
气缸后退
继续输送
```

### 梯形图

梯形图是 PLC 真正执行的程序。

它会把上面的流程变成：

```text
X0 启动按钮
X1 停止按钮
X3 物料检测
X4 气缸前进到位
X5 气缸后退到位

Y0 输送电机
Y1 气缸前进阀
Y2 气缸后退阀

M100 自动运行
M101 输送步骤
M102 气缸前进步骤
M103 气缸后退步骤
```

所以关系是：

```text
流程图 = 动作设计
梯形图 = 程序实现
I/O 点位 = 程序和真实设备之间的接口
```

---

## 十、AI 在 PLC 学习中的作用

AI 对你最有用的地方不是“直接替你干活”，而是帮你把看不懂的东西翻译成人话。

AI 可以帮你：

```text
解释 PLC 基础概念
解释梯形图截图
把梯形图翻译成 C 语言伪代码
整理 X/Y/M/T/D 地址
分析某个 M 位什么时候置位、什么时候复位
根据工艺需求生成梯形图草案
生成调试清单
检查常见风险
写学习笔记
```

AI 不适合直接做：

```text
直接生成最终程序并下载到 PLC
直接判断现场设备是否安全
直接替代师傅或电气工程师确认
直接控制电机、气缸、伺服、加热等危险设备
```

---

## 十一、你应该怎么向 AI 提问

不要这样问：

```text
帮我写 PLC 程序。
```

这个问题太大，AI 容易乱写。

应该这样问：

```text
我是 PLC 新手，使用三菱 GX Works 软件。
请用 C 语言类比解释这段梯形图。

我看到的元件有：
M62
T22 K10
RST M62
SM400
M900
ZRST M100 M107

请输出：
1. 每个元件是什么意思
2. 每一行逻辑
3. C 语言伪代码
4. 这段程序可能用于什么场景
5. 哪些地方必须结合现场确认
```

---

## 十二、最推荐的 AI 使用流程

### 第一步：让 AI 看截图

把 PLC 梯形图截图发给 AI。

问：

```text
请逐行解释这张 PLC 梯形图。
我是新手，请用简单语言说明。
最后请翻译成 C 语言伪代码。
看不清的地方请明确说不确定，不要猜。
```

---

### 第二步：让 AI 整理软元件

问：

```text
请把截图里出现的所有 X、Y、M、T、D 地址整理成表格。
表格包含：
地址
类型
可能含义
在程序中的作用
不确定点
```

---

### 第三步：让 AI 追踪一个状态位

比如你想看 `M62`。

问：

```text
我在程序里搜索 M62，看到它在这些地方出现：
1. SET M62
2. M62 启动 T22 K10
3. T22 到时 RST M62

请分析 M62 的生命周期：
什么时候 ON
什么时候 OFF
控制了什么
有没有可能无法复位
```

---

### 第四步：让 AI 根据需求设计草案

问：

```text
我使用三菱 PLC 和 GX Works。
请根据下面需求生成 PLC 梯形图草案。

输入点：
X0 启动按钮
X1 停止按钮
X2 急停
X3 物料检测
X4 气缸前进到位
X5 气缸后退到位

输出点：
Y0 输送电机
Y1 气缸前进电磁阀
Y2 气缸后退电磁阀
Y3 报警灯

动作要求：
按启动后输送电机运行。
检测到物料后电机停止，气缸前进。
前进到位后延时 2 秒。
然后气缸后退。
后退到位后电机继续运行。
按停止或急停时全部输出关闭。

请输出：
1. I/O 表
2. M 位分配
3. 自动流程步骤
4. 梯形图文字版
5. C 语言伪代码
6. 调试步骤
7. 风险检查
```

---

### 第五步：让 AI 帮你检查风险

问：

```text
请检查下面 PLC 控制逻辑是否有风险：
1. 是否有重复线圈
2. 是否有 SET 后没有 RST
3. 是否有气缸前进和后退同时输出风险
4. 是否有急停后输出没有关闭
5. 是否有报警无法复位
6. 是否有步骤位同时 ON
7. 是否有超时报警缺失
```

---

## 十三、0 基础第一周学习计划

### 第 1 天：认识 PLC 和软件界面

学习内容：

```text
PLC 是什么
梯形图是什么
X/Y/M/D/T/C 是什么
常开触点、常闭触点是什么
OUT、SET、RST 是什么
```

目标：

```text
看到一行简单梯形图，能大概知道条件和输出。
```

---

### 第 2 天：理解 PLC 扫描周期

学习内容：

```text
PLC 如何循环读取输入、执行程序、刷新输出
为什么梯形图是反复执行的
为什么 SET/RST 会保持状态
```

目标：

```text
能用 while 循环理解 PLC。
```

---

### 第 3 天：看懂现有项目结构

学习内容：

```text
AUTO 程序块
INPUT 程序块
OUTPUT 程序块
ALARM 程序块
COMMENT 注释
```

目标：

```text
知道每个程序块大概负责什么。
```

---

### 第 4 天：学会追踪一个 M 位

学习内容：

```text
搜索 M 位
看它在哪里 SET
看它在哪里 RST
看它控制了哪些输出
```

目标：

```text
能分析一个内部状态的完整生命周期。
```

---

### 第 5 天：理解自动流程

学习内容：

```text
自动运行状态
步进流程
M100、M101、M102 这类步骤位
步骤切换条件
```

目标：

```text
能把自动流程理解成状态机。
```

---

### 第 6 天：用 AI 生成一个小练习

练习内容：

```text
启动按钮
停止按钮
物料检测
气缸前进
气缸后退
输送电机
报警灯
```

目标：

```text
让 AI 生成梯形图草案，然后自己在软件里照着画。
```

---

### 第 7 天：处理真实项目

工作方式：

```text
拿真实截图给 AI 解释
整理 I/O 和 M 位
找出要修改的逻辑
让 AI 生成修改方案
人工确认
再去软件里改
```

---

## 十四、一个完整 PLC 小项目示例

### 需求

```text
按启动按钮后，输送电机运行。
物料传感器检测到物料后，电机停止。
气缸前进推出物料。
前进到位后等待 1 秒。
气缸后退。
后退到位后，电机继续运行。
停止按钮或急停时全部停止。
气缸前进超过 3 秒不到位报警。
气缸后退超过 3 秒不到位报警。
```

### I/O 表

| 地址 | 名称 |
|---|---|
| X0 | 启动按钮 |
| X1 | 停止按钮 |
| X2 | 急停 |
| X3 | 物料检测 |
| X4 | 气缸前进到位 |
| X5 | 气缸后退到位 |
| Y0 | 输送电机 |
| Y1 | 气缸前进阀 |
| Y2 | 气缸后退阀 |
| Y3 | 报警灯 |

### M 位分配

| 地址 | 含义 |
|---|---|
| M100 | 自动运行 |
| M101 | 输送步骤 |
| M102 | 气缸前进步骤 |
| M103 | 前进到位等待 |
| M104 | 气缸后退步骤 |
| M900 | 总报警 |

### 定时器分配

| 地址 | 含义 |
|---|---|
| T0 | 前进到位后等待 1 秒 |
| T1 | 气缸前进超时 3 秒 |
| T2 | 气缸后退超时 3 秒 |

### C 语言伪代码

```c
if (stop || emergency) {
    auto_run = false;
    step_conveyor = false;
    step_extend = false;
    step_wait = false;
    step_retract = false;
}

if (start && !stop && !emergency && !alarm) {
    auto_run = true;
    step_conveyor = true;
}

if (auto_run && step_conveyor) {
    motor = true;

    if (material_sensor) {
        motor = false;
        step_conveyor = false;
        step_extend = true;
    }
}

if (auto_run && step_extend) {
    cylinder_extend = true;
    cylinder_retract = false;

    if (extend_done) {
        step_extend = false;
        step_wait = true;
    }

    if (extend_timeout) {
        alarm = true;
    }
}

if (auto_run && step_wait) {
    if (timer_1s_done) {
        step_wait = false;
        step_retract = true;
    }
}

if (auto_run && step_retract) {
    cylinder_extend = false;
    cylinder_retract = true;

    if (retract_done) {
        step_retract = false;
        step_conveyor = true;
    }

    if (retract_timeout) {
        alarm = true;
    }
}

motor = auto_run && step_conveyor && !alarm;
alarm_light = alarm;
```

---

## 十五、用 AI 生成 PLC 程序时必须注意的安全问题

PLC 控制的不是页面按钮，而是真实设备。

真实设备可能包括：

```text
电机
气缸
伺服
加热器
切刀
夹具
输送线
升降机构
```

所以一定不能：

```text
AI 生成后直接下载运行
不看接线图就改输出
不确认急停和安全门
不确认前进后退互锁
不确认报警和复位
不确认设备是否有人在附近
```

必须做到：

```text
先看图纸
先查 I/O 点位
先手动测试输入
再测试输出
再测试单步动作
最后测试自动流程
危险动作必须有人现场确认
```

---

## 十六、建议你每天固定这样用 AI

### 学概念

```text
我是 PLC 新手，请用 C 语言类比解释三菱 PLC 的 T 定时器。
```

### 看截图

```text
这是 GX Works 梯形图截图。
请逐行解释，并翻译成 C 语言伪代码。
```

### 查地址

```text
请帮我分析 M100 在程序里的作用。
它在哪里置位，在哪里复位，控制了什么？
```

### 写草案

```text
请根据这个动作流程生成三菱 PLC 梯形图文字版。
```

### 查风险

```text
请检查这个逻辑有没有重复线圈、互锁缺失、超时报警缺失。
```

### 写调试清单

```text
请根据这个 PLC 程序生成现场调试步骤。
```

---

## 十七、推荐给你的固定提问模板

以后你可以直接复制这个模板。

```text
我是 PLC 新手，使用三菱 GX Works 软件。
我有 C 语言基础，但不懂 PLC。

请根据我提供的截图或需求，帮我分析 PLC 程序。

要求：
1. 用新手能理解的话解释
2. 尽量用 C 语言伪代码类比
3. 整理 X/Y/M/T/D 地址
4. 说明每一段逻辑在干什么
5. 标出不确定的地方
6. 提醒需要结合现场确认的内容
7. 不要直接说程序可以上线
```

---

## 十八、最终学习路线

建议按这个顺序学：

```text
1. PLC 是什么
2. 梯形图怎么看
3. X/Y/M/D/T/C 是什么
4. SET/RST/OUT/ZRST 是什么
5. PLC 扫描周期
6. 输入处理
7. 输出处理
8. 自动流程
9. 步进状态机
10. 报警处理
11. 定时器和计数器
12. 手动模式
13. 自动模式
14. 联机监控
15. 现场调试
```

---

## 十九、最后总结

你现在要做的不是一下子成为 PLC 高手。

你现在最重要的是先做到：

```text
看得懂别人写的梯形图
知道 X/Y/M/T/D 是什么
知道程序和现场设备怎么对应
知道怎么用 AI 把梯形图翻译成人话
知道怎么让 AI 生成程序草案
知道程序不能直接无脑下载运行
```

AI 对你最有价值的地方是：

```text
把陌生的 PLC 逻辑翻译成你能懂的解释
帮你快速入门
帮你整理思路
帮你生成草案
帮你检查风险
```

但是最终一定要记住：

```text
AI 生成的是草案，不是最终安全程序。
PLC 控制的是真实机器，必须人工确认、现场调试、安全验证。
```

如果你只记一句话，就记这个：

```text
PLC 可以理解成一个永远循环运行的状态机。
输入 X 进来，程序判断 M/T/D，最后控制输出 Y。
AI 可以帮你看懂和设计这个状态机，但不能替你承担现场安全责任。
```
